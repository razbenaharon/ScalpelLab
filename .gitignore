.idea/data_source_mapping.xml
.idea/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.env
.venv
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.pytest_cache/
.mypy_cache/
.tmp_tests/

# Temporary Claude Code files
tmpclaude-*-cwd
.claude/scheduled_tasks.lock

# NiceGUI runtime storage (app.storage.general etc.)
.nicegui/

# Pipeline logs and generated helper output (local operational output)
logs/
out/

# Local Codex safety backups
.codex_pull_backups/

# Local Claude Code settings (machine-specific tool allowlist, local paths)
.claude/settings.local.json

# Generated pipeline checkpoints: machine-local state holding absolute paths
# into the research data tree. Both loaders recreate them when absent.
docs/seq_idx_repair_tracking.json
docs/redaction_tracking.json

# Private research data. The real catalog names clinical staff, so it never
# leaves this machine; the public repo ships sample_data/ScalpelDatabase_mock.sqlite
# (rebuild with scripts/helpers/build_mock_db.py).
/ScalpelDatabase.sqlite
*.sqlite.bak*
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
private/
*.xlsx
~$*
